import Detail from "@/components/pages/admin/blogs/detail/Detail";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin | Blogs Detail",
  description: "-",
};

const BlogsDetail = () => <Detail />;

export default BlogsDetail;
